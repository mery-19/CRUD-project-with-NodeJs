{{!-- {{#each events}}
<div class="card text-left m-2">
  <img class="card-img-top" src="holder.js/100px180/" alt="">
  <div class="card-body">
    <h4 class="card-title">{{this.title}}</h4>
    <p class="card-text">{{this.description}}</p>
  </div>
</div>
{{/each}} --}}


<div id="accordion">
    {{#each events}}
  <div class="card">
    <div class="card-header" id="{{this.title}}">
      <h5 class="mb-0">
        <button class="btn btn-link" data-toggle="collapse" data-target="#{{this._id}}" aria-expanded="true" aria-controls="{{this._id}}">
            {{this.title}}
        </button>
      </h5>
    </div>

    <div id="{{this._id}}" class="collapse" aria-labelledby="{{this.title}}" data-parent="#accordion">
      <div class="card-body">
          {{this.description}}
      </div>
    </div>
  </div>
 {{/each}}

</div>